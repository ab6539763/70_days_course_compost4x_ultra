# Day 11 作业

扩展 doc_keyword_counter.py：

1. 支持 `--config keywords.json` 从外部文件加载关键词
2. 输出 CSV 报告 `report.csv`（列：file, keyword, count）
3. 新增敏感词检测：命中敏感词时 print 警告（敏感词列表可硬编码 3 个示例词）


## 答案见

courseware/day-11/README.md 底部
