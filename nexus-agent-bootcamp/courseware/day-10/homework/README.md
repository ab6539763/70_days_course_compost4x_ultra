# Day 10 作业

扩展 nexus_cli 包：

1. 新增 `echo` 子命令：`python -m nexus_cli echo hello world` 原样输出
2. 在 exceptions.py 新增 `ConfigError`，用于配置文件缺失场景
3. 编写 `try/except` 演示捕获 `NexusCLIError` 及其子类


## 答案见

courseware/day-10/README.md 底部
