# Day 13 作业

1. 实现 `@timing` 装饰器：记录函数每次执行耗时，写入 `timing.log`
2. 实现 `@cache_result(ttl=60)` 装饰器：60 秒内相同参数返回缓存
3. 用 `asyncio.gather` 并发调用 3 次 `fetch_data`，对比串行耗时


## 答案见

courseware/day-13/README.md 底部
