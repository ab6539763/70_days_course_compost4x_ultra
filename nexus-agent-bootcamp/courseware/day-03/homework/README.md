# Day 3 课后作业

## 任务

扩展 `menu_system.py` 为 `homework/menu_extended.py`：

1. 新增菜单项 `4. 猜数字`（复用 `guess_number` 模块）
2. 新增菜单项 `5. 打印乘法表`（输入 n，1-9）
3. 无效选项连续 3 次后重置计数
4. 使用 `actions` 字典注册，禁止超长 elif 链

## 运行

```bash
cd courseware/day-03
python3 homework/menu_extended.py --test
```

## 参考答案

见 `homework/menu_extended.py`（请先独立完成）
